import SwiftUI

/// Main app entry point — the containing app for the keyboard extension.
/// Provides onboarding, settings, and protocol management UI.
@main
struct SecureChatKeyboardApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
